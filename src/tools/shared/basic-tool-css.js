export const BASIC_TOOL_CSS = `
  .tool-container { max-width: 700px; margin: 0 auto; }
  .tool-container textarea { width: 100%; min-height: 100px; padding: var(--space-3); border: 1px solid var(--color-border); border-radius: var(--radius-md); font-family: monospace; margin-bottom: var(--space-3); }
  .tool-buttons { display: flex; gap: var(--space-3); margin-bottom: var(--space-3); }
`;
