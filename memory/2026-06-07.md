# 2026-06-07

## Session 2 — DRY cleanup, tool deletion, build fix

### Commits (this session)
- `8d4ef83` chore(refactor): extract pixel-tool-utils for 3 image tools
- `6831922` chore(refactor): extract ocr-utils createOcrTool factory
- `4485cc1` chore(refactor): extract image-to-pdf-tool factory (-186 lines)
- `5987700` chore(refactor): fix image-to-pdf width/height bug; +12 unit tests
- `c19288e` chore(refactor): extract pdf-page-browser factory (-78 lines, kills module-level let)
- `c4f3d8d` chore(cleanup): remove extract-text-pdf (duplicate of pdf-to-text); fix broken qr-generator import

### extract-text-pdf deletion (c4f3d8d)
- 280 → 279 tools; PDF category 34 → 33.
- Duplicate of `pdf-to-text` in ocr category (identical description).
- `git rm --cached` stages deletion; orphan stays on disk (Avast).
- `.fallowrc.json` ignorePatterns extended to suppress orphan.
- Registry sync: toolsList.json, tools.json, categories.json, README.md (3 count locations), PROJECT-PLAN.md (file tree + task 2.3.12 + "14 tool files" → 13), update-pdf-status.ps1, fallow-baselines/dupes.json (216→214).
- "Tool Not Found" page correctly served for `/tools/extract-text-pdf` (verified via Chrome).

### Build regression discovered
- `src/tools/qr/qr-generator.js` line 28 imported `downloadDataUrl` from `src/utils/file.js`.
- `downloadDataUrl` was NEVER defined (never existed in file.js — pre-existing bug from b5dbecb JSX→JS conversion).
- vitest didn't catch it; production build (rolldown) caught it as MISSING_EXPORT.
- Fix: removed broken import. Actual downloads use `qrCanvas.toDataURL()` + `URL.createObjectURL()` directly.
- Lesson: **run `npm run build` (not just tests) before commits**. Tests mock happy-dom/canvas and don't validate import resolution.

### Fallow state
- 214 clone groups (was 249, -35 in this session).
- 18.4% duplication grade.
- Top remaining families (mostly design-system/UI scaffolding, not real DRY):
  - page-numbers-pdf + watermark-pdf (85)
  - pdf-to-csv + pdf-to-epub (78)
  - image-filter-tool + watermark-image (56)
  - image-sharpening + pixelate-image (52)
  - format-converter-tool + heic-to-jpg (47)
  - add-text-image + watermark-image (73)
  - add-border-image + round-image-cropper (33)
  - due-date + ovulation-calculator (106, unique business logic)
  - barcode-scanner + qr-scanner (147, NEW top)

### Verification
- 602/602 unit tests pass.
- Production build passes (1.26s, 1137 modules).
- Chrome: /tools/pdf-to-text loads, /tools/extract-text-pdf → "Tool Not Found".

### State at end
- 10 commits pushed this session, all green.
- Tool count: 279.
- Avast-blocked orphans (still on disk): extract-text-pdf.js + 8 stubs + 5 dep dirs + node_modules/.vite.
