# Memory - 2026-07-22

## What Happened

- Built **Browser Fingerprint Checker** tool (`src/tools/privacy/browser-fingerprint-checker.js`) — Phase 28.
- Pure browser APIs, zero network requests, zero dependencies.
- Fingerprinting: canvas hash, WebGL info, font detection (12 fonts), navigator/screen properties, SHA-256 truncated hash.
- Security-critical: grepped file for fetch/XMLHttpRequest/sendBeacon/eval — confirmed zero matches.
- Added null guards for canvas.getContext() and WebGL in jsdom (unit test compatibility).
- 7 unit tests (mocked crypto.subtle for jsdom), 1 Playwright smoke test.
- All checks pass: build, 1599 unit tests, smoke test, SPA performance (<50ms), Fallow (MI 97, CRAP 17.6), Oxlint, Oxfmt.
- Updated registries: tools.json, toolsList.json, categories.json (privacy 8→9), README.md, PROJECT-PLAN.md, tool-building-progress.md, MEMORY.md, TOOLS.md.
- New total: **316 built, 29 planned = 345 total**.
- Commit: `c019f39`.

## Workflow Sync Fix

- User flagged that I used the wrong tool-building workflow (skipped Steps 1-2: web search + webfetch research).
- Root cause: tool-builder skill had off-by-one numbering (1-20) vs AGENTS.md (0-19), and was missing `--changed-since=HEAD~1` flags in Fallow commands.
- Fixed: rewrote `.opencode/skills/tool-builder/SKILL.md` to exactly match AGENTS.md steps 0-19.
- Fixed cross-reference in AGENTS.md ("Step 6" → "Step 5").
- Commit: `41626a8`.

## Expense Splitter Built

- Built **Expense Splitter** tool (`src/tools/finance/expense-splitter.js`) — Phase 28.
- Pure JS + localStorage, zero dependencies.
- Greedy debt simplification algorithm minimizes transaction count.
- Fixed validation: payer always force-included in split (even if unchecked).
- Added validation error messages with red field highlights.
- 14 unit tests, 1 Playwright test. All checks pass.
- New total: **317 built, 28 planned = 345 total**.
- Commit: `0991697`.

## Mind Map Maker Built

- Built **Mind Map Maker** tool (`src/tools/productivity/mind-map-maker.js`) — Phase 28.
- Pure SVG + pointer events + localStorage, zero dependencies.
- Draggable nodes, connector lines, 8 color palette, rename via prompt().
- Export PNG (via Image + canvas) and SVG (via XMLSerializer).
- 10 unit tests, 1 Playwright test. All checks pass.
- New total: **318 built, 27 planned = 345 total**.
- Commit: `0c160b6`.
