# Daily Note - 2026-06-02

## Git Exclusions & Cleanup
- **Issue:** Untracked temporary build directory `dist_old/` showing up in git status and escaping gitignore.
- **Actions:**
  - Updated `.gitignore` to use a wildcard pattern `*dist*/` to permanently exclude any directory containing "dist" in its name (e.g., `dist/`, `dist_old/`, `dist-new/`), preventing any future recurrences.
  - Cleaned up the workspace by force-deleting `dist_old/`.
  - Staged and committed the updated `.gitignore` file.
  - Verified git status is now completely clean.

## Fixed Tool Discrepancies & Implemented Missing Tools
- **Issue:** Found multiple discrepancies between toolsList.json, tools.json, and actual implementations.
- **Actions:**
  1. **Fixed svg-blob-generator status**: Updated from "pending" to "done" in toolsList.json (tool was already implemented).
  2. **Implemented log-anonymizer**: Created `src/tools/dev/log-anonymizer.js` with full functionality for masking sensitive data (IPs, emails, tokens, etc.) in server logs.
  3. **Implemented sql-to-json**: Created `src/tools/dev/sql-to-json.js` for parsing SQL CREATE TABLE and INSERT statements to JSON.
  4. **Updated category counts**: Fixed discrepancies in categories.json:
     - Developer Tools: 11 → 15
     - SEO Tools: 6 → 7
     - Productivity: 4 → 5
  5. **Added sql-to-json to tools.json**: Registered the new tool in the app's runtime data.
  6. **Updated documentation**: Updated README.md and PROJECT-PLAN.md with new tool counts (243 total, 242 built).
- **Testing**: All 214 unit tests pass. Build has permission error (system-level, not related to changes).
