# 2026-08-04

## Fixed: convert-video.js + video-to-audio.js render crash (pre-existing bug)

Both tools were registered "done" but **crashed on load** with:
`TypeError: Cannot read properties of undefined (reading 'innerHTML')` at tool.js

**Root cause:** They used the OLD factory pattern:
```js
const { optionsArea, runFFmpeg } = createVideoTool({ container });
```
But `createVideoTool` now takes a config object (`optionsHTML`, `onProcess`, ...) and **returns a render function**. Destructuring a function yields `undefined`, so `optionsArea.innerHTML +=` threw.

**Fix:** Rewrote both with the current pattern (like `compress-video.js`):
```js
export const render = createVideoTool({
  optionsHTML: `...`,
  actionBtnLabel: "...",
  async onProcess(ffmpeg, inputName, videoInfo, tctx) { ... }
});
export function destroy() {}
```

**Also:** `convert-video.js` used `libvpx-vp9` for WebM output — VP9 encoder crashes the wasm core (see VP9 gotchas in MEMORY.md). Switched to `libvpx` (VP8), confirmed via Context7 the API pattern matches. Option label updated to "WebM (VP8 — best for web)".

## Verification
- Both smoke tests pass (8/8) — would have caught old crash (console error / error-state)
- MCP browser check: both pages render, upload works, options area appears
- E2E convert-video → WebM: real download `converted.webm` (8022 B), valid EBML (1A45DFA3), **V_VP8 track tag confirmed** — VP8 path works
- E2E video-to-audio → MP3: fixture has no audio track so extraction errors gracefully (button resets, 0 console errors) — render bug fixed, no crash
- Unit: 1647/1647 pass · Build OK · oxlint clean · oxfmt applied · fallow clean for changed files
- **No tests exist** for either tool — could add unit/E2E later if desired

## Context7 note
Resolved `/ffmpegwasm/ffmpeg.wasm` — confirmed exec/readFile/Blob flow. Codec choice (VP8 vs VP9) is empirical (tested in browser), not in docs.

## Reminder
Playwright MCP is sandboxed to `D:\Projects\xtoolbox` + `.playwright-mcp`; copy fixtures into workspace to upload, delete after.

## Kanban Board tool: built, verified, approved, docs synced

- Built `src/tools/productivity/kanban-board.js` (Phase 28). Approved by user ("works").
- Design: 3 default columns (To Do / In Progress / Done), add/rename/delete cards, descriptions, column colors (click the palette icon), editable column names, card counts, move via select dropdown OR HTML5 drag & drop, localStorage autosave (`kanban_v1`), reset button. `escapeHtml` on all user data; `showToast` for feedback.
- Verification chain all green: build OK, unit 1677/1677 (kanban 30 tests), smoke 8/8, Playwright 6/6 (Chromium + Mobile Chrome; Firefox/WebKit not installed), SPA perf all routes <50ms, fallow dupes 0.1% (only test helper), oxlint 0 errors, oxfmt applied.
- Chrome DevTools MCP caught + fixed: form fields missing name attr (a11y), label-content-name-mismatch on "Add Card" buttons (renamed to "Add Card to {col}", removed plus sign), hint text said "Double-tap" but code is single-click. Lighthouse a11y = 100.
- Notables: move-select and drag-drop both call the same `moveCard`; re-render deferred via requestAnimationFrame; `renameColumn` trims before truthiness check.
- Registry: kanban-board flipped planned->done in `src/data/tools.json` + `toolsList.json`. Counts now 327 built / 18 planned = 345. Syncs: README badge+total+Phase28 (17/36 -> 19/36)+structure, PROJECT-PLAN total+planned list+header count, memory/tool-building-progress.md header+Phase28 counts+row, TOOLS.md. home.js/footer.js/categories.json need no edit (dynamic / already match).
- Registry verify: total 345, catSum 345, done-but-no-file [], category-mismatches [].
- Commits: `87cc4f0` tool + `3b94fb9` docs + `838f873` AGENTS/skill renumber, then `3c2a102` CodeRabbit fixes (below).

## Kanban Board: 7 CodeRabbit findings fixed + committed (`3c2a102`)

All findings from CodeRabbit review addressed in `src/tools/productivity/kanban-board.js`:
1. Move select: neutral placeholder `Move to…` (value="" selected disabled), current column excluded, `if (!targetColumnId) return` guard.
2. `saveState` now returns true/false; new `persist()` helper shows error toast on storage failure; all mutation handlers use it.
3. Copy fixes: step "Edit a column name to rename it"; FAQ "Can I move cards with my keyboard?".
4. Removed global `root()` ID lookup — `root` threaded through `renderCard`; IDs renamed `kanban-root`→`kanban-board-root`, `kanban-add-col`→`kanban-board-add-column`, `kanban-reset`→`kanban-board-reset`.
5. New exported `adjustDropIndex(board, columnId, cardId, index)` — decrements placeholder index when dropping in same column below source.
6. `loadState` filters null/non-object columns+cards before mapping; default fallback only when ALL records invalid (preserves empty `columns: []`).
7. Replaced undefined/hardcoded CSS vars (`--color-background-primary`, `--radius`, `--text-md`) with real tokens.css tokens (`--color-bg`, `--color-surface`, `--radius-md`, `--radius-sm`, `--radius-full`, `--text-base`, `--text-xs`, `--shadow-sm`, `--space-*`).

Verification: unit 1687/1687 (+10 new regression tests incl. adjustDropIndex cases), build OK, smoke 8/8, browser MCP confirmed move-select live (0 console errors, 0 4xx). oxlint/oxfmt clean. fallow dupes 0.1% pre-existing.

Branch: main ahead of origin/main by 7 commits — **nothing pushed yet**.

## Phase 28 count reconciliation: removed duplicate `timezone-converter` from registry

- CodeRabbit flagged: README Phase 28 "19/36" → 17 remaining vs PROJECT-PLAN header "Planned Tools (18)". Root cause: `dev/timezone-converter` was still registered as `planned` in `src/data/tools.json` + `toolsList.json` even though `memory/tool-building-progress.md` documents it as ❌ duplicate of done `world-clock`.
- User decision: **remove it from the registry** (keep tracker note as documentation).
- Changes: removed entry from `src/data/tools.json` + `toolsList.json` (id lists still in sync), dev count 40→39 in `src/data/categories.json` + `README.md` (Categories table + Project Structure tree) + `PROJECT-PLAN.md` (Categories table), total 345→344 / planned 18→17 in README.md (tagline, hero, total line) + PROJECT-PLAN.md (Current Status + "Planned Tools (17)" header) + TOOLS.md + memory/tool-building-progress.md header. Removed `timezone-converter` spec section from PROJECT-PLAN.md. `.opencode/skills/tool-researcher/SKILL.md` Dev: 40→39.
- Phase 28 row stays "19/36" (36 = 37 total − 1 duplicate; 19 built + 17 planned = 36). README's "345 tools" and badge (327 built) now consistent with registry.
- Verify: build OK, unit 1687/1687, registry total 344 = 327 done + 17 planned, catSum 344, no id drift, dev actual 39 = categories.json.

