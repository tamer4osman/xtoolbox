# 2026-08-07

## json-schema-validator (Phase 28, dev) — COMPLETED ✅

Built and fully verified `src/tools/dev/json-schema-validator.js`. Validates JSON documents against JSON Schema **Draft-07** (default) and **2020-12** (via `ajv/dist/2020`), Ajv bundled through npm.

### Workflow (21-step convention)

1. **Duplicate check** — `json-schema-validator` was already registered as `planned` in `src/data/tools.json` + `toolsList.json` with no implementation file → in-place flip planned→done, NOT a net-new tool. Totals stay 343.
2. **Grilled design** — approved: two side-by-side textareas (schema + data), draft dropdown, "Validate" button + optional "Validate as you type" (default OFF, 400ms debounce), Load example button, per-error path/line/column detail. Ajv (MIT, ~150KB) per PROJECT-PLAN JSON spec line 925.
3. **Implementation** — helpers `parseJson(text,label)`, `findKeyLine(text,key)`, `formatAjvError(error)`, `validateWithSchema(schemaText,dataText,draft="07"|"2020-12")`. Fixed intermediate bugs: `parseJsonSchema`→`parseJson`, dropped duplicate `additionalProperty` branch, removed dead `getJsonLineInfo`.
4. **Security** — no raw fetch/eval/new Function; stripped `http://json-schema.org/draft-07/schema#` from example schema (rule surfaced this build — no `http://` strings). Example kept but valid without `$schema`.
5. **Unit** — `src/__tests__/json-schema-validator.test.js` 13/13 (parseJson empty/invalid, findKeyLine, formatAjvError required/additionalProp/nested, validateWithSchema valid/violation/schema-error/2020-12/invalid-schema).
6. **Playwright** — `tests/json-schema-validator.spec.js` 2 tests.
7. **Verify chain** — build PASSED; vitest 13/13; smoke 0 console errors; SPA perf all 10 routes ≤50ms; fallow dupes/health clean (no new CRAP>200); oxlint/oxfmt clean.
8. **Chrome DevTools MCP** — loaded tool, 0 console errors, `ajv.js` + `ajv_dist_2020.js` + tool module all 200; evaluate confirmed valid=true and violations `root [additionalProperties]` + `/name [type]`.
9. **User approved** — "works".

### Docs synced (330 built, 13 planned = 343)

- `src/data/tools.json` + `toolsList.json`: status `planned`→`done`, description "Validate JSON data against JSON Schema Draft 07 and 2020-12.", icon 🧩, keywords.
- `README.md`: Total (line 78, 330/13), badge, structure comment, Phase 28 row 21/37→**22/37** (line 124).
- `TOOLS.md`: line 7 (330 built, 13 planned).
- `PROJECT-PLAN.md`: line 148 (330 built, 13 planned), "Planned Tools (13)", removed json-schema-validator from Phase 28 remaining dev line (line 184).
- `memory/tool-building-progress.md`: header line 3 (330/13), Phase 28 header line 5 (22 built/13 planned), JSON Schema Validator row Planned→✅ Done.
- Category counts UNCHANGED: dev `toolCount` 38 (flip only, was already counted). Verify: tools.json dev=38, categories dev=38, done 330, planned 13. footer.js/home.js dynamic via `availableTools` + `categories.json.toolCount` — no hardcoded edits needed.

### Notes for future

- Registry-status flips (planned→done) do NOT change category `toolCount` (both already counted there) — only change the done/planned totals in prose.
- Windows shell has no `grep`/`tail` — use the Grep tool or `node -e` for line checks.
- Ajv loads two chunks (`ajv.js`, `ajv_dist_2020.js`) when 2020-12 draft is supported via dynamic import — both served 200.

### Git state

- Not yet committed. Uncommitted: json-schema-validator.js + test + spec (new), tools.json/me toolsList.json/README.md/TOOLS.md/PROJECT-PLAN.md/memory /tool-building-progress.md/package.json/package-lock.json/ (ajv deps). Awaiting user approval before commit.